# SmartBin_plastic_biological
Smart waste-sorting bin built with Raspberry Pi 5 that recognizes different types of waste using a CNN trained on a custom dataset. Based on the classification, the appropriate lid opens automatically. The system also supports remote lid control via a Telegram chatbot.
